class PatientNode:  # Define una nueva clase llamada PatientNode
    def __init__(self, data):  # Define el método inicializador (constructor) de la clase
        self.data = data  # Asigna el valor del parámetro 'data' al atributo 'data' del objeto
        self.next = None  # Inicializa el atributo 'next' del objeto a None, indicando que no hay siguiente nodo

